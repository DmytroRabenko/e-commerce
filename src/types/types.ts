import { ReactNode } from 'react';

export interface ContainerProps {
    children: ReactNode;
  }




  
export interface Product {
  name: string;
  type: string;
  height: string;
  id: string;
  color: string;
  price: string;
  count: string;
  code: string;
  url: string;
}